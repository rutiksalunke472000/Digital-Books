
export default class Book{
    constructor(public bookId: number, public bookLogo:string, public category:String, public title:string, public publisher:string,
        public publishedDate: String, public price: number, public content:string, public active: string, public author:string ){}
}